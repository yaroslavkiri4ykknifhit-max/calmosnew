import React from 'react';
import { ResponsiveContainer, AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip, Legend } from 'recharts';
import { Metric } from '../types';
import { format, parseISO } from 'date-fns';
interface Props{data:Metric[];dataKeys:{key:string;color:string;label:string}[];height?:number;}
const CT=({active,payload,label}:any)=>{if(!active||!payload)return null;return(<div className="glass-card !p-3 !rounded-xl shadow-xl"><p className="text-xs text-surface-400 mb-2">{label}</p>{payload.map((p:any)=>(<div key={p.dataKey} className="flex items-center gap-2 text-sm"><div className="w-2 h-2 rounded-full" style={{background:p.color}}/><span className="text-surface-300">{p.name}:</span><span className="font-semibold text-white">{p.value}</span></div>))}</div>);};
export default function TrendChart({data,dataKeys,height=300}:Props){const cd=data.map(m=>({...m,date:format(parseISO(m.date),'MMM dd')}));return(<ResponsiveContainer width="100%" height={height}><AreaChart data={cd} margin={{top:5,right:5,left:-20,bottom:5}}><defs>{dataKeys.map(dk=>(<linearGradient key={dk.key} id={`g-${dk.key}`} x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor={dk.color} stopOpacity={0.3}/><stop offset="95%" stopColor={dk.color} stopOpacity={0}/></linearGradient>))}</defs><CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,0.05)"/><XAxis dataKey="date" tick={{fill:'#94a3b8',fontSize:11}} axisLine={{stroke:'rgba(255,255,255,0.1)'}} tickLine={false}/><YAxis tick={{fill:'#94a3b8',fontSize:11}} axisLine={{stroke:'rgba(255,255,255,0.1)'}} tickLine={false}/><Tooltip content={<CT/>}/><Legend wrapperStyle={{paddingTop:'10px'}} formatter={(v:string)=><span className="text-surface-300 text-xs">{v}</span>}/>{dataKeys.map(dk=>(<Area key={dk.key} type="monotone" dataKey={dk.key} name={dk.label} stroke={dk.color} strokeWidth={2} fill={`url(#g-${dk.key})`} dot={false} activeDot={{r:4,strokeWidth:2,stroke:dk.color,fill:'#0f172a'}}/>))}</AreaChart></ResponsiveContainer>);}
