import React, { useState } from 'react';
import { Link, useLocation, useNavigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
import { Brain, LayoutDashboard, PenLine, History, Settings, LogOut, Menu, X, Sparkles } from 'lucide-react';
export default function Navbar(){const{user,logout}=useAuth();const loc=useLocation();const nav=useNavigate();const[open,setOpen]=useState(false);const items=[{path:'/dashboard',label:'Dashboard',icon:LayoutDashboard},{path:'/entry',label:'New Entry',icon:PenLine},{path:'/history',label:'History',icon:History},{path:'/settings',label:'Settings',icon:Settings}];const doLogout=async()=>{await logout();nav('/');};if(!user)return null;return(<><nav className="fixed top-0 left-0 right-0 z-50 glass border-b border-white/10"><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"><div className="flex items-center justify-between h-16"><Link to="/dashboard" className="flex items-center gap-2 group"><div className="w-9 h-9 bg-gradient-to-br from-calm-400 to-zen-400 rounded-xl flex items-center justify-center shadow-lg shadow-calm-500/20"><Brain className="w-5 h-5 text-white"/></div><span className="text-xl font-bold gradient-text hidden sm:block">CalmOS</span></Link><div className="hidden md:flex items-center gap-1">{items.map(n=>{const I=n.icon;const a=loc.pathname===n.path;return<Link key={n.path} to={n.path} className={`flex items-center gap-2 px-4 py-2 rounded-xl text-sm font-medium transition-all ${a?'bg-calm-500/20 text-calm-300':'text-surface-400 hover:text-white hover:bg-white/5'}`}><I className="w-4 h-4"/>{n.label}</Link>;})}</div><div className="hidden md:flex items-center gap-3"><div className="flex items-center gap-2 px-3 py-1.5 glass rounded-xl"><Sparkles className="w-3.5 h-3.5 text-zen-400"/><span className="text-xs text-surface-400">{user.email}</span></div><button onClick={doLogout} className="p-2 text-surface-400 hover:text-danger-400 rounded-xl"><LogOut className="w-4 h-4"/></button></div><button onClick={()=>setOpen(!open)} className="md:hidden p-2 text-surface-400">{open?<X className="w-5 h-5"/>:<Menu className="w-5 h-5"/>}</button></div></div></nav>{open&&<div className="fixed inset-0 z-40 md:hidden"><div className="absolute inset-0 bg-black/60" onClick={()=>setOpen(false)}/><div className="absolute top-16 left-0 right-0 glass border-b border-white/10 p-4 animate-slide-down"><div className="flex flex-col gap-2">{items.map(n=>{const I=n.icon;return<Link key={n.path} to={n.path} onClick={()=>setOpen(false)} className={`flex items-center gap-3 px-4 py-3 rounded-xl font-medium ${loc.pathname===n.path?'bg-calm-500/20 text-calm-300':'text-surface-400'}`}><I className="w-5 h-5"/>{n.label}</Link>;})}<hr className="border-white/10 my-2"/><button onClick={()=>{doLogout();setOpen(false);}} className="flex items-center gap-3 px-4 py-3 rounded-xl text-danger-400 font-medium"><LogOut className="w-5 h-5"/>Sign Out</button></div></div></div>}</>);}
