import React, { useState } from 'react';
import { Link, useNavigate, Navigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
import { Brain, Mail, Lock, UserPlus, Loader2, Check } from 'lucide-react';
import toast from 'react-hot-toast';
export default function SignUp(){const{user,signUp}=useAuth();const nav=useNavigate();const[email,setEmail]=useState('');const[pw,setPw]=useState('');const[cf,setCf]=useState('');const[sub,setSub]=useState(false);if(user)return<Navigate to="/dashboard" replace/>;const checks=[{label:'8+ chars',met:pw.length>=8},{label:'Has number',met:/\d/.test(pw)},{label:'Match',met:pw.length>0&&pw===cf}];const handle=async(e:React.FormEvent)=>{e.preventDefault();if(!email||!pw||!cf){toast.error('Fill all');return;}if(pw!==cf){toast.error('No match');return;}try{setSub(true);await signUp(email,pw);toast.success('Welcome!');nav('/dashboard');}catch{toast.error('Failed');}finally{setSub(false);}};return(<div className="min-h-screen flex items-center justify-center px-4 relative"><div className="absolute inset-0 overflow-hidden"><div className="absolute top-1/3 right-1/4 w-96 h-96 bg-zen-500/10 rounded-full blur-3xl"/></div><div className="w-full max-w-md relative animate-scale-in"><div className="text-center mb-8"><Link to="/"><div className="w-12 h-12 bg-gradient-to-br from-calm-400 to-zen-400 rounded-2xl flex items-center justify-center shadow-lg shadow-calm-500/30 mx-auto mb-6"><Brain className="w-7 h-7 text-white"/></div></Link><h1 className="text-3xl font-bold text-white mb-2">Create Account</h1><p className="text-surface-400">Start your journey</p></div><div className="glass-card"><form onSubmit={handle} className="space-y-5"><div><label className="block text-sm font-medium text-surface-300 mb-2">Email</label><div className="relative"><Mail className="absolute left-3 top-1/2 -translate-y-1/2 w-5 h-5 text-surface-500"/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="your@email.com" className="input-field pl-11"/></div></div><div><label className="block text-sm font-medium text-surface-300 mb-2">Password</label><div className="relative"><Lock className="absolute left-3 top-1/2 -translate-y-1/2 w-5 h-5 text-surface-500"/><input type="password" value={pw} onChange={e=>setPw(e.target.value)} placeholder="••••••••" className="input-field pl-11"/></div></div><div><label className="block text-sm font-medium text-surface-300 mb-2">Confirm</label><div className="relative"><Lock className="absolute left-3 top-1/2 -translate-y-1/2 w-5 h-5 text-surface-500"/><input type="password" value={cf} onChange={e=>setCf(e.target.value)} placeholder="••••••••" className="input-field pl-11"/></div></div><div className="space-y-2">{checks.map(c=><div key={c.label} className={`flex items-center gap-2 text-xs ${c.met?'text-zen-400':'text-surface-500'}`}><Check className={`w-3.5 h-3.5 ${c.met?'opacity-100':'opacity-30'}`}/>{c.label}</div>)}</div><button type="submit" disabled={sub} className="btn-primary w-full flex items-center justify-center gap-2">{sub?<Loader2 className="w-5 h-5 animate-spin"/>:<><UserPlus className="w-4 h-4"/>Create</>}</button></form><div className="mt-6 text-center"><p className="text-surface-400 text-sm">Have account? <Link to="/login" className="text-calm-400 font-medium">Sign In</Link></p></div></div><p className="text-center text-xs text-surface-600 mt-6">Demo — any email/password</p></div></div>);}
